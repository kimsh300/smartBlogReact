# Portfolio Builder

포트폴리오 웹사이트를 쉽게 만들 수 있는 웹빌더 스타일 플랫폼입니다.

## 🚀 기술 스택

### Frontend
- **React 18** + **TypeScript**
- **React Router DOM** - 라우팅
- **React i18next** - 다국어 지원 (한국어/영어)
- **React Infinite Scroll Component** - 무한 스크롤
- **Vite** - 빌드 도구

### Backend (예정)
- **Spring Boot** (전자정부프레임워크)
- **MySQL** - 메인 데이터베이스
- **Redis** - 캐싱

## 📁 프로젝트 구조

```
portfolio-builder/
├── src/
│   ├── components/
│   │   ├── layout/          # 레이아웃 컴포넌트 (Header, Sidebar, Footer)
│   │   ├── common/          # 공통 UI 컴포넌트
│   │   ├── pageBuilder/     # 웹빌더 관련 컴포넌트
│   │   └── userProfile/     # 사용자 프로필 컴포넌트
│   ├── pages/               # 페이지 컴포넌트
│   ├── styles/              # CSS 스타일 파일
│   ├── hooks/               # 커스텀 훅
│   ├── utils/               # 유틸리티 함수
│   ├── types/               # TypeScript 타입 정의
│   ├── i18n/                # 다국어 설정 및 번역 파일
│   ├── data/                # 샘플 데이터 (API 연동 전)
│   └── assets/              # 정적 파일
├── public/
├── package.json
├── tsconfig.json
├── vite.config.ts
└── README.md
```

## 🎨 주요 기능

### 1. 홈 페이지
- 포트폴리오를 등록한 사용자들의 프로필 카드 목록
- 무한 스크롤로 더 많은 사용자 로드
- 사용자 검색 기능

### 2. 포트폴리오 페이지
- 왼쪽 사이드바 네비게이션
- 현재 섹션 하이라이트
- 스크롤 기반 섹션 이동

### 3. 웹빌더 스타일 레이아웃
- **1:1** - 풀스크린 단일 컬럼
- **1:2** - 좌측 좁게, 우측 넓게
- **2:1** - 좌측 넓게, 우측 좁게
- **2:2** - 2단 동일 분할

### 4. 반응형 디자인
- 데스크톱 (1024px+)
- 태블릿 (768px - 1024px)
- 모바일 (768px 이하)

### 5. 다국어 지원
- 한국어 (기본)
- 영어

## 🛠️ 설치 및 실행

```bash
# 의존성 설치
npm install

# 개발 서버 실행
npm run dev

# 프로덕션 빌드
npm run build

# 빌드 미리보기
npm run preview
```

## 📝 TODO (Spring Boot 연동)

코드에 `TODO:` 주석으로 표시된 부분은 Spring Boot API 연동이 필요합니다:

### API 엔드포인트 (예정)
- `GET /api/users` - 사용자 목록 (페이지네이션)
- `GET /api/users/search?q={query}` - 사용자 검색
- `GET /api/portfolios/{username}` - 포트폴리오 조회
- `GET /api/portfolios/{id}/nav` - 네비게이션 아이템
- `GET /api/portfolios/{id}/sections` - 섹션 데이터
- `POST /api/auth/login` - 로그인
- `POST /api/auth/signup` - 회원가입

### Redis 캐싱 적용 예정
- 사용자 목록
- 포트폴리오 데이터
- 검색 결과

## 🎯 개발 가이드

### CSS 구조
모든 CSS는 `src/styles/` 폴더에서 관리합니다:
- `global.css` - CSS 변수 및 전역 스타일
- `layout.css` - 레이아웃 컴포넌트 스타일
- `components.css` - 공통 UI 컴포넌트 스타일
- `home.css` - 홈 페이지 스타일
- `portfolio.css` - 포트폴리오 페이지 스타일
- `pageBuilder.css` - 페이지 빌더 스타일

### 다국어 추가
`src/i18n/locales/` 폴더에 새 언어 파일을 추가하고 `src/i18n/index.ts`에 등록합니다.

## 📜 라이센스

MIT License
